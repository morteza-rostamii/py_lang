<!-- 

# pip install requests flask

# create requirements.txt with all packages installed
# pip freeze > requirements.txt

# clone the project

# activate the env
source env/bin/activate
# install packages
pip install -r requirements.txt

# update the requirements.txt anytime install or update a new package
pip freeze > requirements.txt



-->